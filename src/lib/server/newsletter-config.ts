// Server-side newsletter configuration.
// Keep shared offer values here instead of env when they are stable app config.

export const newsletterConfig = {
  sharedDiscountCode: 'SUBSCRIBE10',
} as const;
